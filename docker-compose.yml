version: '3'

services:
  frontend:
    build:
      context: ./client   
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./client:/app

  backend:
    build:
      context: ./CodingTime.API  
      dockerfile: Dockerfile 
    ports:
      - 7227:7227